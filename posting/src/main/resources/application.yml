spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blog_posting
    username: postgres
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
    open-in-view: false
